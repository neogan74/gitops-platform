apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-app-go
spec:
  tls:
  - hosts:
    - staging-demo.local
    secretName: staging-demo-app-go-tls
  rules:
  - host: staging-demo.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: staging-demo-app-go
            port:
              name: http
